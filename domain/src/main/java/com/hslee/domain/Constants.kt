package com.hslee.domain

object Constants {
    object NetWork {
        const val CONNECT_TIMEOUT: Long = 30
        const val WRITE_TIMEOUT: Long = 30
        const val READ_TIMEOUT: Long = 30
    }

    object ViewType {
        const val CONTENT: Int = 0
        const val TITLE: Int = 1
    }
}